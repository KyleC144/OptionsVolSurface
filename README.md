# OptionsVolSurface


## Overview


## Methodology


## Results


## Tech Stack
TypeScript, JavaScript, Python, Pandas, NumPy, SciPy, Yfinance

## Usage
Navigate in the terminal into the vol-surface directory via `cd vol-surface`. Then install all dependancies and set everything up by running the setup.sh script via `./setup.sh`. Then you can run `npm run dev` in the terminal to start the web application and api.
