{
  "name": "vol-surface",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"node server.js\" \"vite\"",
    "build": "vite build",
    "start": "NODE_ENV=production node server.js",
    "demo": "NODE_ENV=production DEMO=true node server.js",
    "capture": "node scripts/capture.js",
    "build:demo": "mkdir -p public/demo-data && cp -r demo-data/* public/demo-data && VITE_DEMO=true vite build && rm -rf public/demo-data"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}